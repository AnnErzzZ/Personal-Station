/**
 * 11 / 12「按角色配置权限，控制系统复杂度」的数据模型。
 *
 * 2026-09-23 章节重构（用户指令）：本章唯一主视觉 = 配置中心「权限管理」页
 * 真实截图（permission-management.png），其余内容全部围绕主界面收拢：
 *
 *   Section Header（一句核心说明）
 *   → Large Product Stage（真实权限配置主界面）
 *   → 三项机制（01 功能入口 / 02 操作权限 / 03 数据范围，各一句说明）
 *   → 低权重 Caption
 *
 * 03 章已详细讲「谁在使用系统、完成什么任务」，本章只回答
 * 「这些角色如何通过权限配置看到不同的系统」，不重复介绍角色。
 *
 * ── 事实边界（docs/cases/heyispace-os.md §0 / §A / §F / §G / §H / §J-4）──
 * - 权限能力范围取自 §J-4 与 §F「说明不同用户的可见与操作范围」；
 * - 数据范围按「项目与空间」表述，与 04 章 §I 决定一致，不写死房间粒度；
 *   权限管理页截图中不含数据范围授权，故主界面不做 Focus 标注（无法
 *   准确对应 03 数据范围），主界面保持静态。
 * - 不出现人数、项目数量、权限效率提升、安全收益；不新增组织架构、
 *   审批流程与复杂 RBAC 矩阵；不重绘假的权限管理后台。
 * - 配图为配置中心「权限管理」页的真实截图（2026-09-23 补入
 *   11-role-access/permission-management.png），不是为本章新造的界面。
 *   发布前待办：截图右上角含账号名，按 §J-9 复核是否脱敏。
 */

/**
 * 三项核心机制：编号 + 标题 + 一句说明（用户 2026-09-23 定稿文案）。
 * 放在产品主界面下方，作为主界面的轻量注解，不再做独立的三块大内容。
 */
export const ACCESS_DIMENSIONS = [
  {
    id: "capability",
    index: "01",
    name: "功能入口",
    lead: "按角色开放所需的导航与功能。",
  },
  {
    id: "operations",
    index: "02",
    name: "操作权限",
    lead: "区分查看、编辑与配置能力。",
  },
  {
    id: "scope",
    index: "03",
    name: "数据范围",
    lead: "限定可访问的项目与空间数据。",
  },
] as const;

export type AccessDimensionId = (typeof ACCESS_DIMENSIONS)[number]["id"];

/** 章节末尾的低权重 Caption（用户 2026-09-23 指定；「按项目」无原图支撑，
 *  收敛为截图可直接支撑的「按角色逐项配置」口径）。 */
export const ACCESS_CLOSING = "具体权限由管理员按角色逐项配置。";
